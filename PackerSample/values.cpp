#include "shared.h"

// Actual definitions of values
// Should be const
// Now worker without being const, some time ago didin't
// Too lazy to dig for more info why

const PackedBuffer value_test = { MARK_TEST, "default" };
const PackedBuffer value_another = { MARK_ANOTHER, "default#2" };
